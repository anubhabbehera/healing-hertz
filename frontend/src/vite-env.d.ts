/// <reference types="vite/client" />
// Declares Vite's ambient module types (CSS side-effect imports, asset
// imports, import.meta.env). TypeScript 7 rejects `import "./styles.css"`
// without these.
